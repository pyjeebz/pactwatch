openapi: "3.0.3"
info:
  title: Test API
  version: "1.0.0"
paths:
  /orders:
    post:
      summary: Create order
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - product_id
              properties:
                product_id:
                  type: string
      responses:
        "201":
          description: Created
